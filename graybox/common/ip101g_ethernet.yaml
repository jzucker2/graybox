# https://esphome.io/components/ethernet.html
# this is for IP101G
# https://docs.m5stack.com/en/unit/poesp32
ethernet:
  type: IP101
  mdc_pin: GPIO23
  mdio_pin: GPIO18
  clk_mode: GPIO0_IN
  phy_addr: 1
  power_pin: GPIO5
